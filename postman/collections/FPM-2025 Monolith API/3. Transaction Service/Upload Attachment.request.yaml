$kind: http-request
url: "{{base_url}}/transactions/{{transaction_id}}/attachments"
method: POST
headers:
  Authorization: Bearer {{token}}
body:
  type: formdata
  content:
    - type: file
      key: file
      src:
        - ""
order: 5000
