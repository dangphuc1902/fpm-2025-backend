# Handle line endings consistently
* text=auto

# Treat all shell/batch files as text
*.sh text eol=lf
*.cmd text eol=crlf
*.bat text eol=crlf

# Java files
*.java text diff=java

# XML / YAML
*.xml text
*.yml text
*.yaml text

# Ignore case sensitivity differences
* text=auto eol=lf
