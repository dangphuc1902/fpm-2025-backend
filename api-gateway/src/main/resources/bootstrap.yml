spring:
  cloud:
    config:
      uri: http://localhost:8888
      uri: http://config-server:8888 # Cập nhật để trỏ đến tên dịch vụ trong Docker Compose
      fail-fast: true
      name: api-gateway
      profile: default
  application:
    name: api-gateway