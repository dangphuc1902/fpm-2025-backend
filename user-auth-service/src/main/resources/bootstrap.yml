spring:
  cloud:
    config:
      uri: ${SPRING_CLOUD_CONFIG_URI:http://localhost:8888}
      fail-fast: true
      name: user-auth-service
      profile: default
  application:
    name: user-auth-service