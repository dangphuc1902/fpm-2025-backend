spring:
  application:
    name: fpm-monolith
  profiles:
    active: monolith
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:fpm_db}?currentSchema=public
    username: ${DB_USER:dev}
    password: ${DB_PASS:secret}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      idle-timeout: 30000
      connection-timeout: 30000
      max-lifetime: 1800000
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        temp:
          use_jdbc_metadata_defaults: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
  main:
    allow-bean-definition-overriding: true

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET:supersecretkeyforfpm2025mustbelongenoughotherwiseitwillthrowanexception}

app:
  jwt:
    refreshTokenExpirationMs: 604800000

monolith:
  security:
    permit-all:
      - "/api/v1/auth/**"
      - "/swagger-ui/**"
      - "/api-docs/**"
      - "/actuator/**"
